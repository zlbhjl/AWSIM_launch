# ==========================================
# uturn_config.py
# Uターンシナリオの自動テスト（ファジング）設定ファイル
# 配置場所: ~/AWSIM_launch/uturn_config.py
# ==========================================

# 1. 実行するシナリオの種類（将来別のシナリオが増えたらここを変える）
SCENARIO_TYPE = "uturn"

# 2. 全体の実行回数（何回ループさせるか）
REPEAT_COUNT = 3000

# 3. ランダム生成するパラメータの範囲 (最小値, 最大値)
# --------------------------------------------------------
# 実行のたびに、この範囲内からランダムな数値（小数点）が選ばれます。
# これにより「たまたま事故が起きるエッジケース」を自動で発掘します。
PARAM_RANGES = {
    # Uターン開始のトリガー距離 dx0 (m)
    # 小さいほど自車の目の前で急旋回する危険なテストになります
    "dx0": (10.0, 25.0),         

    # 自車(Ego)の目標速度 _ego_speed (km/h)
    "ego_speed": (20.0, 40.0),   

    # 他車(NPC)の走行・Uターン速度 _npc_speed (km/h)
    "npc_speed": (10.0, 25.0),   
}

# 4. 固定するパラメータ（ランダムにせず、毎回同じ値を使うもの）
# --------------------------------------------------------
# シナリオの「場所」や「初期配置」に関するパラメータです。
FIXED_PARAMS = {
    # 自車(Ego)の配置
    "ego_init_lane": "511",
    "ego_init_offset": 38,
    "ego_goal_lane": "513",
    "ego_goal_offset": 20,
    
    # 他車(NPC)の配置
    "npc_init_lane": "521",
    "npc_init_offset": 32,
    "uturn_next_lane": "511",    # Uターン後の合流先車線
    
    # その他の固定設定
    "acceleration": 7.0          # NPCの加速度
}
