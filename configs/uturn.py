# ==========================================
# uturn.py (AI検証・統合設定ファイル)
# ==========================================

SCENARIO_TYPE = "uturn"

# run_manager.py の大元のループ回数 (AI側のMAX_SAMPLESより大きくしておく必要があります)
REPEAT_COUNT = 3000

# ==========================================
# 1. AI 探索・終了条件の設定
# ==========================================
# ターゲットの優先順位（厳しい順に上から記述）
TARGET_PRIORITIES = [
    "c_collision",
    "c_ttc_0.7",
    "c_ttc_1.2",
    "c_ttc_1.5"
]

# AIの動作フェーズと終了判定の閾値
INITIAL_EXPLORATION_LIMIT = 100  # 初期Sobol探索（ランダムな基礎データ収集）の回数
MIN_SAMPLES = 500                # 終了判定を開始する最低実行回数
MAX_SAMPLES = 2000               # 強制終了する最大実行回数 (セーフティネット)
TOLERANCE_GRAY_ZONE = 0.01       # 許容するグレーゾーンの割合 (1% = 0.01)


# ==========================================
# 2. シミュレーション パラメータ空間の設定
# ==========================================
# AIが探索（変動）させるパラメータの範囲
PARAM_RANGES = {
    "dx0": (10.0, 25.0),         
    "ego_speed": (30.0, 40.0),   
    "npc_speed": (10.0, 25.0),   
}

# 常に固定するパラメータ（AIはいじらない）
FIXED_PARAMS = {
    "ego_init_lane": "514", 
    "ego_init_offset": 30,  
    "ego_goal_lane": "516",
    "ego_goal_offset": 20,

    "npc_init_lane": "521",
    "npc_init_offset": 32,
    # ★自車と同じ 514 レーンに合流させて意図的に衝突リスクを作る
    "uturn_next_lane": "514", 
    
    "acceleration": 7.0
}
